# Catalyst Development Deployment Ledger

## Controlled target

- Project: `KSPDatathon2026`
- Project ID: `43492000000013049`
- Organization/environment ID: `60077844198`
- Environment: Development only
- Data classification: synthetic only

Secrets, Catalyst tokens, session material, personal invitation addresses, HMAC key values, and future identity-provider credentials are never recorded in this ledger or committed to Git.

## Checkpoints

| IST time | Commit | Checkpoint | Remote mutation | Resources/IDs | Verification | Rollback | Verdict |
|---|---|---|---|---|---|---|---|
| 2026-07-20 20:14:30 IST | `8a47eeb` | Pre-scaffold Catalyst CLI state | None; local Function scaffolding only | CLI `1.27.0`; project `43492000000013049`; branch `codex/catalyst-development` | Remote preflight passed; clean tree; Development and synthetic-only locks active | Delete uncommitted local scaffold files | PASS |
| 2026-07-20 21:10:14 IST | `08aa290` | Authorize intelligence schema creation | Create exactly 21 additive tables from `schema/catalyst/intelligence-schema.json` in Development; do not alter the existing 29 tables; seed no rows | API bundle `7F1C208B70E344DDF4C9FB235EDCDF741A3166DABABBF78A50FACA088580EA13`; Job bundle `D71705BD2792DE0610E01FDAB0E2321BE7D25338CADC84F7796F835766308171` | 141 tests passed; both schema validators passed; both bundles passed; clean remote preflight passed; challenge review from `main` passed semantic inspection | If verification fails, delete only newly created empty tables in reverse load order; never touch the existing 29 tables | AUTHORIZED |
| 2026-07-20 22:17:04 IST | `a456560` | Verify intelligence schema creation | Created 21 additive Development tables and 220 manifest columns; corrected the empty `INT_NetworkEdge.NetworkEdgeID` column by recreating it with the required unique constraint; inserted no rows; denied Select/Update/Insert/Delete to App User on every new table | Table IDs below; export job `43492000000063001`; ZIP SHA-256 `EF1BB413D78A6C5F5BC193EE12F80409C991FE0AE35B74D7C710CB4A07470D50` | Fresh Catalyst IaC export passed a strict combined comparison: all 50 Development tables and 611 manifest columns match the source and intelligence manifests, including foreign keys, parent `ROWID`, `ON-DELETE-SET-NULL`, types, lengths, defaults, uniqueness, mandatory, search-index and PII settings; 21/21 permissions pages independently showed all App User table permissions denied | Delete only these 21 empty additive tables in reverse load order if this checkpoint must be rolled back; never alter the existing 29 source/ingestion tables | PASS |
| 2026-07-20 22:20:13 IST | `f78c6c4` | Authorize targeted Function deployment | Deploy only `crime_intelligence_api` and `intelligence_refresh` to Development so their Function resources exist; do not invoke bootstrap, create schedules, configure Authentication/API Gateway, seed data or deploy Production | API manifest SHA-256 `7F1C208B70E344DDF4C9FB235EDCDF741A3166DABABBF78A50FACA088580EA13`; Job manifest SHA-256 `D71705BD2792DE0610E01FDAB0E2321BE7D25338CADC84F7796F835766308171` | 141 tests passed; both schema validators and both bundle inspections passed; combined 50-table export comparison passed; challenge alignment and clean Development remote preflight passed | Delete only the two newly deployed Development Functions if targeted deployment verification fails; never modify tables or Production resources | AUTHORIZED |
| 2026-07-20 22:36:22 IST | `4ea848a` | Verify targeted Function deployment | Deployed the exact authorized API and Job bundles to Development; configured the five required Development environment-variable names through attended Catalyst configuration; never read or recorded the HMAC value; invoked no Job and seeded no rows | API `crime_intelligence_api` ID `43492000000065001`; Job `intelligence_refresh` ID `43492000000066001`; API URL `https://kspdatathon2026-60077844198.development.catalystserverless.in/server/crime_intelligence_api/` | Catalyst reported both targeted deployments successful; API and Job configuration each exposed all five required key names; an initially invalid attended secret failed safely with sanitized `500 INTERNAL_ERROR`; after the user replaced it with a compliant value, the approved unauthenticated brief route returned `401 UNAUTHENTICATED`, JSON, `no-store`, a request ID and no stack or operational details | Delete only Function IDs `43492000000065001` and `43492000000066001` and their Function configuration if rollback is required; do not alter Data Store or Production | PASS |
| 2026-07-20 22:55:59 IST | `1e1d43a` | Authorize one synthetic bootstrap | Created Development Function Job Pool `IntelligenceRefreshPool` with 512 MB; authorize exactly one `BOOTSTRAP_SYNTHETIC` submission using batch key `KSP-DEMO-20260720-V1`, seed `20260720` and `syntheticOnly=true`; no cron or recurring schedule | Job Pool ID `43492000000052011`; Job Function ID `43492000000066001`; source SHA-256 `b600047d4dbe36e64d37303810638925efa0cb56d07c61a6aaf71668d19a761b`; reconciliation SHA-256 `bb4513c72170fb5c0a9d0cf4d65cd78e7cf3769d2dc15472faade0bc43decc31`; expected input-manifest SHA-256 `7e9ff193c14a9ff691635ba899bea89662c3f43465a3b343e7f38a0cae9df468` | Pool is Function type and 512 MB, above the target Function's 256 MB; 0 prior jobs; expected source is 26 entities, 50 unique `CaseMaster` rows and 411/411 accepted rows with 0 rejects and balanced reconciliation; 141 tests, both schema validators, intelligence evaluation, bundle build/inspection, challenge alignment and clean remote preflight passed | On an interrupted submission, retry only the same idempotent batch key; never submit a second batch key or delete source/intelligence rows automatically; investigate and reconcile before any manual rollback | AUTHORIZED |
| 2026-07-20 23:26:22 IST | `6bd4541` | Authorize refresh observability redeploy | DevOps Logs enabled with user approval; first Job `43492000000064016` failed during startup because the Job Function lacked its separate `KSP_ENVIRONMENT` configuration; all five Job Function key names were then configured without reading the secret; same-batch recovery Job `43492000000064025` reached the Function but failed without a safe phase event; authorize redeploy of only `intelligence_refresh` with phase/code-only failure telemetry | Refresh manifest SHA-256 `50DACC374539BF896F14E0B32120CEE73659F2CB5C09367AE050FCEECADE2D70`; Job Function ID `43492000000066001`; both failed Jobs used batch key `KSP-DEMO-20260720-V1` and zero retries | 142 tests passed; both schema validators and both bundle inspections passed; telemetry test proves logs exclude SDK message, secret and stack; existing logs show the first failure occurred before handler/Data Store access; no successful bootstrap is claimed | Redeploy the prior verified refresh bundle if telemetry deployment fails; retry only the same batch key after reading the safe phase; never create a new batch key or delete rows automatically | AUTHORIZED |
| 2026-07-20 23:42:43 IST | `debd8e0` | Authorize repository-boundary diagnostic redeploy | The observability deployment cleared Function-specific variables. Job `43492000000064029` consequently failed at runtime configuration. After restoring all five Development key names, Job `43492000000064038` reached `SERVICE_EXECUTION` and failed safely. Read-only inspection proved `INT_AnalysisRun`, `TRN_IngestionBatch`, `SRC_CaseMaster`, and `TRN_SourceKeyMap` remain empty. Authorize redeploy of only `intelligence_refresh` with fixed, data-free progress markers around refresh lookup, source persistence, batch staging, and publication. Do not submit a Job until the five Function key names are restored and verified after deployment | Refresh manifest SHA-256 `5AC43E0751DC73261A96892CB8B3C34DA5A376CAA030DEDE4AED27B71FD05146`; Job Function ID `43492000000066001`; same authorized batch key `KSP-DEMO-20260720-V1`; no cron and zero retries | 143 tests passed; both schema validators and both bundle inspections passed; focused red/green test proves a repository failure reports only `REFRESH_BATCH_LOOKUP` plus a stable code and never the underlying Data Store detail; four remote checkpoint tables are empty, so no partial bootstrap state requires reconciliation | Redeploy the prior verified refresh bundle if deployment fails; because a redeploy clears Job variables, restore and verify the five required Development key names before any same-batch diagnostic run; never create a new batch key or delete rows automatically | AUTHORIZED |
| 2026-07-21 00:25:01 IST | `524fab7` | Authorize Catalyst temporal-boundary redeploy | Repository-boundary deployment succeeded and its five Function key names were restored. Same-batch diagnostic Job `43492000000064051` used the exact authorized parameters and zero retries, then failed safely at `SOURCE_PERSIST` in 898 ms. All four bootstrap checkpoints remained empty. Catalyst's official Data Store contract requires DateTime values in `YYYY-MM-DD HH:MM:SS`; authorize redeploy of only `intelligence_refresh` with Date/DateTime boundary serialization and omission of the unset optional `CompletedAt` field | Refresh manifest SHA-256 `B1ACEC5279184E655A8E8BD4D874689825517107C4745108AB794B335405E171`; Job Function ID `43492000000066001`; same batch key `KSP-DEMO-20260720-V1`; no cron and zero retries | Two focused tests failed first on ISO values, then passed after conversion; 144 tests passed; both schema validators and both Function bundle inspections passed; raw PDF values still drive provenance hashes; challenge review PASS | Redeploy only the prior verified refresh bundle if deployment fails; restore and verify the five required Development key names after deployment; run only the same authorized batch key; if any row is written, reconcile before another attempt | AUTHORIZED |
| 2026-07-21 00:51:24 IST | `ccf3f86` | Authorize intelligence DateTime-boundary redeploy | The temporal source fix was deployed and all five Function key names restored. Same-batch Job `43492000000064059` used the exact authorized parameters and zero retries. It completed source persistence, then failed safely at `REFRESH_BATCH_STAGE`. Read-only reconciliation found one completed synthetic ingestion batch with 26 files, 411/411 accepted rows, zero warnings/rejects, 50 linked `SRC_CaseMaster` FIRs, and no `INT_AnalysisRun` rows. Authorize redeploy of only `intelligence_refresh` with centralized Catalyst DateTime serialization for intelligence/workflow writes and omission of unset optional DateTimes | Refresh manifest SHA-256 `A3A7FD9335179C9D9CAFC3D88B58E3E65C71A6DB328132AB66FB2CE170D31FFC`; Job Function ID `43492000000066001`; same batch key `KSP-DEMO-20260720-V1`; no cron and zero retries | Focused repository test failed first on ISO DateTimes, then passed; 144 tests passed; both schema validators and both Function bundle inspections passed; challenge review `docs/reviews/2026-07-21-catalyst-intelligence-datetime-boundary.md` is PASS; remote source reconciliation is balanced and intelligence staging is empty | Redeploy only the prior verified refresh bundle if deployment fails; restore and verify the five required Development key names after deployment; retry only the same batch key so the completed source batch is reused; reconcile any intelligence rows before another attempt | AUTHORIZED |
| 2026-07-21 10:01:19 IST | `904b391` | Verify PDF-semantic batch reset | After an exact read-only dry run and explicit user approval, deleted only synthetic rows belonging to quarantined Development batch `KSP-DEMO-20260720-V1` (`ROWID 43492000000075002`) in reverse dependency order. No Function was deployed, no Job was submitted, and no Production resource was touched | Project `43492000000013049`; 26 source tables; `TRN_SourceKeyMap`; `TRN_RejectedRecord`; `TRN_IngestionBatch`; no `INT_AnalysisRun` existed | Dry run proved 411 source rows, 411 key mappings, one batch row, zero rejects and zero analysis runs. Deleted exactly those 823 rows; Catalyst applied the key-map deletion in capped chunks of 300 and 111. A fresh independent count sweep returned zero for all 26 source tables, the key map, rejected records, the exact batch row and the batch's analysis runs | The invalid batch is intentionally absent. Reload only by deploying the verified corrected `intelligence_refresh` bundle, restoring required Function variable names, and submitting the same batch key once with zero retries; do not restore the invalid rows | PASS |
| 2026-07-21 10:30:21 IST | `904b391` | Accept corrected PDF-semantic batch | Deployed only `intelligence_refresh` to Development, restored the five required Function key names without reading the private audit-key value, and submitted exactly one zero-retry `BOOTSTRAP_SYNTHETIC` Job using the existing batch key, seed `20260720`, and `syntheticOnly=true` | Function `43492000000066001`; Pool `43492000000052011`; Job `43492000000064695`; corrected batch ROWID `43492000000068005`; bundle manifest SHA-256 `EB8D2D69EF787D582D55049BBB239DB6C80BAD4F295B52F5413BD7BC9DB9DF6A` | Job succeeded; 411/411 source rows accepted with zero rejects; 50 FIRs; 411 source-key mappings; 7 analysis runs; all 50 CrimeNo/CaseNo derivations, station-scoped identities and timelines passed; persisted outputs include 1 hotspot, 1 anomaly, 1 pattern, 2 repeat signals, 1 area risk, 58 network edges and 1 actionable alert | Redeploy this verified bundle and repeat only the same idempotent batch key if infrastructure recovery is required; do not create a second demo batch, recurring schedule, or Production resource | PASS |
| 2026-07-21 17:27:00 IST | `16fb889` | Authorize intelligence-workspace Development deployment | Create exactly seven additive empty tables (`CFG_ReportDefinition`, `CFG_Dashboard`, `CFG_DashboardItem`, `CFG_ContentShare`, `CFG_UserPreference`, `WF_AlertNote`, `WF_Escalation`); deploy only `crime_intelligence_api`; deploy the reviewed React SPA through Catalyst Slate; configure or verify only the 33 reviewed authenticated API operations when the existing API Gateway state permits this without weakening security. Do not redeploy or invoke `intelligence_refresh`; do not submit a Job, create a cron, load data, alter existing tables, enable public signup, or touch Production | Project `43492000000013049`; API Function `43492000000065001`; API manifest SHA-256 `A749FAD3E8148991593A5AD7676BC9E94AF56D9F494E0A22404C5EBA498F24A3`; reviewed refresh manifest remains `14AB8030C30643EED3385E2334636237BA05C96114EF1E505367976E8740FA63` but is not authorized for deployment | API and refresh bundles build and inspect successfully with 34 and 37 files respectively; authorization remains conditional on a fresh 197-test suite, both schema validators, web test/build, exact-project clean preflight, challenge-alignment PASS, post-creation schema/permissions comparison, authenticated route protection, and deployed browser smoke | Restore the prior API bundle if its smoke test fails; remove only this Slate Development deployment and the seven new tables if they remain empty; never delete or modify the accepted synthetic batch, the existing 21 intelligence tables, source tables, or Production resources | AUTHORIZED |

## Intelligence table IDs

| Table | Catalyst table ID | Table | Catalyst table ID |
|---|---:|---|---:|
| `CFG_UserAccess` | `43492000000054003` | `TRN_CaseFeature` | `43492000000054362` |
| `TRN_LocationFeature` | `43492000000054721` | `TRN_PersonResolution` | `43492000000055080` |
| `TRN_DistrictContext` | `43492000000055439` | `INT_AnalysisRun` | `43492000000055798` |
| `INT_Hotspot` | `43492000000056157` | `INT_Anomaly` | `43492000000056516` |
| `INT_Pattern` | `43492000000056875` | `INT_AreaRisk` | `43492000000057234` |
| `INT_NetworkNode` | `43492000000057593` | `INT_NetworkEdge` | `43492000000057952` |
| `INT_RepeatOffenderSignal` | `43492000000058311` | `INT_FindingEvidence` | `43492000000058670` |
| `WF_Alert` | `43492000000059029` | `WF_Command` | `43492000000059388` |
| `WF_AlertEvidence` | `43492000000059747` | `WF_Assignment` | `43492000000060106` |
| `WF_AnalystConclusion` | `43492000000060465` | `WF_Outcome` | `43492000000060824` |
| `WF_AuditEvent` | `43492000000061183` |  |  |

The evidence ZIP and extracted Catalyst template remain under ignored `artifacts/catalyst-development/` paths. No source or intelligence rows and no Production resources were created at this checkpoint.
