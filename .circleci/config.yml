version: 2.1

jobs:
  verify:
    docker:
      - image: cimg/node:24.18.0
    steps:
      - checkout
      - restore_cache:
          keys:
            - npm-v1-node24-{{ checksum "package-lock.json" }}
      - run: npm ci
      - save_cache:
          key: npm-v1-node24-{{ checksum "package-lock.json" }}
          paths:
            - ~/.npm
      - run: npm run verify
      - run: npm run compat:node18
      - run: npm run geospatial:verify

workflows:
  pull-request-verification:
    jobs:
      - verify
