import { commandCenterDestinations } from './command-center-navigation.js';
export function CommandCenterRail({ selected, onSelect, onDashboardOpen = () => {} }) { return <nav className="command-center-rail" aria-label="Command Center modules">{commandCenterDestinations.map(({ id, label, icon: Icon }) => <button key={id} type="button" aria-label={label} aria-current={selected === id ? 'page' : undefined} onClick={() => { onSelect(id); if (id === 'dashboards') onDashboardOpen(); }}><Icon aria-hidden="true" /></button>)}</nav>; }
