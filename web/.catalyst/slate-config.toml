# Generated and managed by the Catalyst CLI for Slate deployment.

framework = "react-vite"
install_command = "npm install"
build_path = "dist"
build_command = "npm run build"
deployment_name = "default"
root_path = "./"
